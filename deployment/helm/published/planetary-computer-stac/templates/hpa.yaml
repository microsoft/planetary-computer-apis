apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "pcstac.fullname" . }}
  labels:
    {{- include "pcstac.labels" . | nindent 4 }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "pcstac.fullname" . }}
  minReplicas: {{ .Values.stac.hpa.minReplicas }}
  maxReplicas: {{ .Values.stac.hpa.maxReplicas }}
  behavior:
    scaleDown:
      stabilizationWindowSeconds: 30
  metrics:
  - type: Pods
    pods:
      metric:
        name: http_requests_per_minute
      target:
        type: AverageValue
        averageValue: 500m